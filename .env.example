# ================================
# GitHub Agent 配置示例
# ================================

# 基础平台配置
PLATFORM=github

# ================================
# GitHub 基础配置
# ================================

# GitHub Personal Access Token（需要 repo 完整权限）
# 获取地址: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Webhook 签名验证密钥（自定义强密码）
# 必须与 GitHub App 中设置的 Webhook Secret 一致
WEBHOOK_SECRET=your_strong_webhook_secret_here

# ================================
# GitHub App 配置（用于 @mention 支持）
# ================================

# GitHub App ID（在 App 设置页面获取）
GITHUB_APP_ID=123456

# 私钥文件路径（下载的 .pem 文件）
GITHUB_APP_PRIVATE_KEY_PATH=./your-app-private-key.pem

# Client ID（在 App 设置页面获取）
GITHUB_APP_CLIENT_ID=Iv1.a1b2c3d4e5f6g7h8

# Client Secret（需要在 App 设置页面生成）
GITHUB_APP_CLIENT_SECRET=your_generated_client_secret

# GitHub App 名称（用于 @mention，必须与实际 App 名称一致）
GITHUB_APP_NAME=your-agent-name

# ================================
# LLM / AI 配置
# ================================

# LLM API 基础 URL
LLM_BASE_URL=https://api.openai.com/v1/chat/completions

# LLM API 密钥
LLM_API_KEY=sk-your_openai_api_key_here

# 使用的模型（推荐 gpt-3.5-turbo 或 gpt-4）
LLM_MODEL=gpt-3.5-turbo

# ================================
# 安全配置（可选但推荐）
# ================================

# 限制允许使用的用户（用逗号分隔，留空表示允许所有用户）
ALLOWED_USERS=

# 限制允许使用的仓库（用逗号分隔，留空表示允许所有仓库）
ALLOWED_REPOS=

# ================================
# 开发调试配置
# ================================

# 调试模式（生产环境请设为 false）
DEBUG=true

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# ================================
# 演示模式配置（可选）
# ================================

# 演示模式下预设的文件定位结果
# DEMO_LOCATE_FILES=src/auth.py,src/login.py

# 演示模式下预设的修复方案
# DEMO_PROPOSE_PLAN=Fix null pointer in auth validation

# 演示模式下预设的修复内容
# DEMO_FIX_CONTENT=Added null check in validateUser function
